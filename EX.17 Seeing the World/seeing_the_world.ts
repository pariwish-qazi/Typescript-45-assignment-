let places :string[] = ['USA', 'Japan', 'London', 'Disney World', 'Times Square'];

console.log('My favorite places are ' + places);
console.log('Copy ' + [...places].sort());

// The originalarray is still in its original order by printing it.
console.log('My favorite places are ' + places);
//array in reverse alphabetical order
console.log('Copy' + [...places].sort().reverse());
// The originalarray is still in its original order by printing it.
console.log('My favorite places are ' + places);
//Reversed the order of your list and printed it
console.log('My favorite places are' + places.reverse());
console.log( 'My favorite places are ' + places.reverse());
//Sorted  array so it’s stored in alphabetical order
console.log( 'My favorite places are ' + places.sort());
// Sort to change array so it’s stored in reverse alphabetical order.
console.log('My favorite places are ' + places.sort().reverse());

